{% extends 'base.html.twig' %}

{% block title %}Guinot-immobilier{% endblock %}

{% block body %}

/ Accueil
    <div class = "jumbotron">
        <br> GUINOT IMMOBILIER:</br>
        <br> ACCESS LOCATION & VENTE D'APPARTEMENTS</br>
    </div>

<div class="container">

    {# Affichage de mon Bien#}

    <affichage>
      {# Faisant une interpollation#}
        <h1> {{ immobilier.titre }}</h1>
            <p> Date de publication: {{ immobilier.createdAt | date('d/m/Y')}}</p>
             <div class = "contenu">
                <img src="{{ immobilier.photo}}/850x300" alt=""  align="left">
                 <p align="justify"> {{ immobilier.description}}   </p> 
                 <p align="justify"> {{ immobilier.description}}   </p> 
                 <p align="justify"> {{ immobilier.description}}   </p> 
      {#           <a href="{{ path('index.affich', {'id': immobilier.id })}}" class="btn btn-primary"> Consulter</a> #}  
            </div>     

                    <a href="{{ path('immo.edition', {'id': immobilier.id}) }}"><button type="button" class="btn btn-infoin">Editer</a>
                    {{ include('all/_delete_form.html.twig') }}
                    
    </affichage>
          <hr> </hr>

</div>

{% endblock %}
